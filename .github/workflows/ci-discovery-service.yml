name: CI Discovery Service

on:
    push:
        paths:
            - 'discovery-service/**'
    pull_request:
        paths:
            - 'discovery-service/**'

jobs:
    ci:
        uses: ./.github/workflows/reusable-ci.yml
        with:
            service-path: 'discovery-service'
        secrets:
            SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}